# This is what makes the jar a Quarkus extension: the Quarkus bootstrap reads it from the
# runtime artifact, resolves the deployment artifact named here and runs its build steps.
#
# group and version are expanded by processResources, so this stays in step with the build.
deployment-artifact=${group}:quarkus-typesafe-deployment:${version}
